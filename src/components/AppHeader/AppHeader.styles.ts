const styles = {
  appBar: {
    backgroundColor: 'var(--mui-palette-custom-inputs)',
    borderBottom: '1px solid',
    borderColor: 'var(--mui-palette-custom-badges)',
  },
  toolbar: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    px: { xs: 1, sm: 8 },
  },
};

export default styles;
